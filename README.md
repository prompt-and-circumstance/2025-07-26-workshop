# Prompt and Circumstance Workshop

## Development

First, you need to paste your API keys into the `.env` file.

```sh
cp .env.example .env
```

Paste the values of the API keys which have been provided to you separately.

Then, from your terminal:

```sh
pnpm install
pnpm dev
```

This starts your app in development mode, rebuilding assets on file changes.
